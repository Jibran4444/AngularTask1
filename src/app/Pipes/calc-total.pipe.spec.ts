import { CalcTotalPipe } from './calc-total.pipe';

describe('CalcTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
